import React, { Component } from 'react';

export default class Transaction extends Component {
    render () {
        const actions = "Some transaction";

        return (
            <div>
                {actions}
            </div>
        );
    }
}
